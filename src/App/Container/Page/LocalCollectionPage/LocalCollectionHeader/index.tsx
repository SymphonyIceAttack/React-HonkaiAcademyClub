import React from "react";
import Style from "./LocalCollectionHeader.module.less"
const index = () => {
  return <div className={`${Style.LocalCollectionHeader}`}>index</div>;
};

export default index;
