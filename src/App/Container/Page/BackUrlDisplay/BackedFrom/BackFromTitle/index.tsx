import React from "react";
import Style from "./BackFromTItle.module.less";
const index = () => {
    return (
        <div className={`${Style.BackFromTitle}`}>
            <h1>后端地址指定</h1>
        </div>
    );
};

export default index;
